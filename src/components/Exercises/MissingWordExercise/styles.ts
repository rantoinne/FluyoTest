import {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  containerStyle: {
    flex: 1,
    width: '100%',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
});

export default styles;
